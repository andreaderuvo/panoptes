# Runtime
fastapi>=0.115,<1
uvicorn[standard]>=0.30,<1
httpx>=0.27,<1          # how every machine is asked
pyyaml>=6.0,<7

# Tests
pytest>=8.0,<10
pytest-asyncio>=0.23,<2
segno>=1.6,<2        # optional: only for `--qr`
